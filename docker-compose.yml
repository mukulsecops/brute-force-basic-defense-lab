services:

  app:
    container_name: brute_app
    build: ./login-app
    command: python app.py
    ports:
      - 5000:5000
    volumes:
      - ./login-app:/app
      - ./logs:/app/logs

  attacker:
    container_name: brute_attacker
    build: ./attacker